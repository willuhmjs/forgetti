{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false
    },
    "dev:mock": {
      "cache": false
    },
    "preview": {},
    "format": {
      "outputs": ["**/*.{ts,tsx,md}"]
    },
    "test": {}
  }
}
