{
"name": "utilities",
  "private": true,
  "scripts": {
    "dev:mock": "bash -c 'command -v ustreamer >/dev/null 2>&1 || { echo >&2 \"ustreamer is not installed. Installing...\"; sudo apt-get install -y ustreamer; }; ustreamer --blank=test.jpg'"

  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}